# Proxmox API Configuration
# Copy this file to .env and fill in your actual values

# Proxmox server URL (include port if not standard)
PROXMOX_URL=https://proxmox-host.example.com:8006

# Proxmox user (format: username@realm)
PROXMOX_USER=ansible@pve

# API Token ID (create in Datacenter > API Tokens)
PROXMOX_TOKEN_ID=example-token-id

# API Token Secret (shown only once when creating token)
PROXMOX_TOKEN_SECRET=00000000-0000-0000-0000-000000000000

# Internal domain for LXC containers
PROXMOX_DOMAIN=example.com